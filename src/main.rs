mod naive_pyra;
pub use crate::naive_pyra::*;
mod dyn_pyra;

fn main() {

    if false {
        brute_force_search();
    }

    if false {
        test_graph_from_start_pos();
    }

    test_full_graph();

}
